// main.cpp
// Entry point for the Task Scheduler / Job Queue System.
// Single-threaded simulation: builds a task batch, lets the user pick a
// scheduling strategy (FIFO / Priority / Deadline-EDF), runs a Worker that
// processes tasks sequentially according to that strategy, logs results,
// and prints an ASCII Gantt-style timeline.
// TODO: implement full flow as described in README.md

#include <iostream>

int main() {
    std::cout << "Task Scheduler -- skeleton project. Implementation pending.\n";
    return 0;
}
