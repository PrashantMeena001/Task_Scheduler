#include "Worker.h"

// Worker.cpp
// TODO: implement Worker::run() -- loop while scheduler.hasNext(),
// pop a task, simulate its execution (e.g. based on burstTime),
// record start/end time, and pass the result to Logger.
